{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Main window capabilities (ADR-30002): custom commands + scoped sidecar exec (Windows browser-url only) + plugin permissions for store, dialog, window-state, deep-link and autostart.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default",
    "dialog:default",
    "window-state:default",
    "deep-link:default",
    "autostart:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/timesapp-win-browser-url",
          "sidecar": true,
          "args": [{ "validator": "\\d+" }]
        }
      ]
    }
  ]
}
